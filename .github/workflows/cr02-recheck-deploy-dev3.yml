name: CR-02 Recheck Deploy DEV3

on:
  push:
    branches:
      - dev3
    paths:
      - '.github/workflows/cr02-recheck-deploy-dev3.yml'

jobs:
  verify:
    runs-on: ubuntu-latest
    env:
      SFTP_HOST: ${{ secrets.IONOS_SFTP_HOST }}
      SFTP_USER: ${{ secrets.IONOS_SFTP_USER_DEV3 }}
      SSHPASS: ${{ secrets.IONOS_SFTP_PASSWORD_DEV3 }}
    steps:
      - name: Recheck Deploy DEV3 PHP runtime
        run: |
          set -euo pipefail
          sudo apt-get update -qq
          sudo apt-get install -y -qq sshpass >/dev/null
          cat > cr02-recheck.php <<'PHP'
          <?php
          header('Content-Type: application/json');
          echo json_encode([
              'php_version' => PHP_VERSION,
              'document_root' => $_SERVER['DOCUMENT_ROOT'] ?? null,
          ], JSON_UNESCAPED_SLASHES);
          PHP
          cleanup() {
            printf '%s\n' 'rm /deploy/cr02-recheck.php' | sshpass -e sftp -oBatchMode=no -oStrictHostKeyChecking=no -oUserKnownHostsFile=/dev/null "$SFTP_USER@$SFTP_HOST" >/dev/null 2>&1 || true
          }
          trap cleanup EXIT
          printf '%s\n' 'put cr02-recheck.php /deploy/cr02-recheck.php' | sshpass -e sftp -oBatchMode=no -oStrictHostKeyChecking=no -oUserKnownHostsFile=/dev/null "$SFTP_USER@$SFTP_HOST" >/dev/null
          curl --fail --silent --show-error --max-time 15 'https://deploy-dev3.nerozon.de/cr02-recheck.php'
          echo
